---
description: Agentklar status and orientation — what's in flight, what needs me, how to use it.
---

Run `agentklar status` and show me the result. Then, in 4 lines or fewer,
remind me: (1) which tasks are waiting on MY approval, (2) the board URL, (3)
the slash commands available (`/agentklar-task`, `/agentklar-board`,
`/agentklar-approvals`, `/agentklar-doctor`), and (4) that an agent can never
approve or finish a task — only I can. If `agentklar` is not on PATH, tell me
how to install it (clone + `go build -o ~/.local/bin/agentklar ./cmd/agentklar`).
